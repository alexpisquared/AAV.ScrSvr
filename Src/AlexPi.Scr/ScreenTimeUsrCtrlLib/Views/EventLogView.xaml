<Window x:Class="ScreenTimeUsrCtrlLib.Views.EventLogView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d"
        xmlns:local="clr-namespace:ScreenTimeUsrCtrlLib.Views"
        Title="EventLogView" Height="1450" Width="800" Background="#333" WindowStartupLocation="CenterOwner">
    <Grid>
    <DataGrid ItemsSource="{Binding PageCvs}" Margin="8,0,8,8" Grid.Row="1" AutoGenerateColumns="False" VerticalAlignment="Top" Background="#222" x:Name="dg1"
              AlternatingRowBackground="#555">
      <DataGrid.Columns>
        <DataGridTextColumn Header=" PubXml File " Binding="{Binding ThisTime,        StringFormat='{}{0:HH\\:mm\\:ss}'}" Width="70" FontWeight="Medium" >
          <DataGridTextColumn.ElementStyle>
            <Style TargetType="TextBlock">
              <Setter Property="Margin" Value="0 -8 0 10" />
            </Style>
          </DataGridTextColumn.ElementStyle>
        </DataGridTextColumn>
        <DataGridTextColumn Header=" PubXml File " Binding="{Binding ThisEoi}"        Width="88" FontWeight="Medium" >
          <DataGridTextColumn.ElementStyle>
            <Style TargetType="TextBlock">
              <Setter Property="Margin" Value="0 -8 0 16" />
            </Style>
          </DataGridTextColumn.ElementStyle>
        </DataGridTextColumn>
        <DataGridTextColumn Header=" PubXml File " Binding="{Binding TimeSpent,       StringFormat='{}{0:h\\:mm\\:ss}'}" Width="70" FontWeight="Medium" >
          <DataGridTextColumn.ElementStyle>
            <Style TargetType="TextBlock">
              <Setter Property="Margin" Value="0 +8 0 0" />
            </Style>
          </DataGridTextColumn.ElementStyle>
        </DataGridTextColumn>
        <DataGridTextColumn Header=" PubXml File " Binding="{Binding TimeSpentTotal,  StringFormat='{}{0:h\\:mm\\:ss}'}" Width="70" FontWeight="Medium" >
          <DataGridTextColumn.ElementStyle>
            <Style TargetType="TextBlock">
              <Setter Property="Margin" Value="0 +5 0 0" />
            </Style>
          </DataGridTextColumn.ElementStyle>
        </DataGridTextColumn>
        <DataGridTextColumn Header=" PubXml File " Binding="{Binding ActivityAsIs}"   FontWeight="Medium" />
        <DataGridTextColumn Header=" PubXml File " Binding="{Binding ActivityCorrected}"  FontWeight="Medium" />
        <DataGridTextColumn Header=" ActivityAsIs " Binding="{Binding ActivityAsIs}" Width="*">
          <DataGridTextColumn.ElementStyle>
            <Style TargetType="TextBlock">
              <Setter Property="Foreground" Value="{Binding ColorCode, FallbackValue=#f0f}" />
              <Setter Property="Margin" Value="0" />
            </Style>
          </DataGridTextColumn.ElementStyle>
        </DataGridTextColumn>
      </DataGrid.Columns>
    </DataGrid>
    <Button IsCancel="True" Click="OnClose" VerticalAlignment="Bottom" HorizontalAlignment="Right" />
  </Grid>
</Window>
